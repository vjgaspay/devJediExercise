# Blue Todo (React + Vite)

A simple React to-do app with a clean blue/light-blue UI.

## Features

- Add, complete, and delete tasks
- Filter tasks by All / Active / Completed
- Clear all completed tasks
- Persists tasks in `localStorage`
- Responsive, light-blue themed interface

## Getting started

### 1) Install dependencies

```bash
npm install
```

### 2) Start development server

```bash
npm run dev
```

### 3) Build for production

```bash
npm run build
```

## Suggested next improvements

- Add due dates and priority labels
- Edit tasks inline
- Add drag-and-drop ordering
- Add dark mode toggle
- Sync tasks with a backend (e.g., Supabase/Firebase)
